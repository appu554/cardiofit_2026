# Grafana datasources configuration for L1 Cache monitoring

apiVersion: 1

datasources:
  - name: Prometheus-L1-Cache
    type: prometheus
    access: proxy
    url: http://prometheus-l1:9090
    isDefault: true
    editable: true
# Grafana dashboard provisioning configuration for L1 Cache

apiVersion: 1

providers:
  - name: 'l1-cache-dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
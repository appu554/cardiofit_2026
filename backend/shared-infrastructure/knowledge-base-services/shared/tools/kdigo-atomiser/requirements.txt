# MCP-RAG KDIGO Atomiser Dependencies (V3 - PageIndex Hybrid)
# Architecture: PageIndex MCP (tables/text) + Claude Vision (heatmaps) + PyMuPDF (image rendering)
# NO LlamaCloud/GroundX/Docling - PageIndex provides vectorless reasoning-based retrieval
#
# Install: pip install -r requirements.txt

# Core MCP framework
mcp>=1.0.0

# PageIndex MCP - Reasoning-based PDF retrieval with page citations
# Tree-index structure, no vector DB required, automatic page-level citations
# https://github.com/nicobailey/pageindex
pageindex>=0.1.0

# PDF Image Rendering ONLY (for Claude Vision heatmap analysis)
# NOT used for text extraction - PageIndex handles that
pymupdf>=1.24.0

# LLM Client (Claude for vision heatmaps + rule extraction)
anthropic>=0.25.0

# Data Models
pydantic>=2.0.0

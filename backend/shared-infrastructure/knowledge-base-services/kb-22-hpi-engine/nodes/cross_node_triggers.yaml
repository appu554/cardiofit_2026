# Cross-Node Safety Triggers (F-07)
# Triggers that fire across all HPI nodes regardless of which node is active.
# Loaded by CrossNodeSafety service at startup.

triggers:
  - trigger_id: XN001
    condition: "Q001=YES AND Q007=YES"
    severity: IMMEDIATE
    recommended_action: "Concurrent chest pain and dyspnea features. Consider ACS with pulmonary congestion. STAT ECG, troponin, BNP."

  - trigger_id: XN002
    condition: "Q009=YES AND Q003=YES"
    severity: URGENT
    recommended_action: "Unilateral leg swelling with bilateral ankle oedema. Evaluate for DVT alongside heart failure assessment."

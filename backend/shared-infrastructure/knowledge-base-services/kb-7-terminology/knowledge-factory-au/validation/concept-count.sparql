# SPARQL Query: Count total concepts in AU kernel
# Used for validation and manifest generation

PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX owl: <http://www.w3.org/2002/07/owl#>

SELECT (COUNT(DISTINCT ?concept) AS ?total_concepts) WHERE {
  ?concept rdf:type owl:Class .
}

# Worklist engine parameters.

sort_weights:
  escalation_tier_base:
    SAFETY: 5000
    IMMEDIATE: 4000
    URGENT: 3000
    ROUTINE: 1000
    INFORMATIONAL: 0
  pai_score_multiplier: 10
  trajectory_rising_boost: 50
  attention_gap_per_day: 2
  transition_active_boost: 30

refresh_interval_seconds: 300

tier_mapping:
  CRITICAL:
    - "SAFETY"
    - "IMMEDIATE"
  HIGH:
    - "URGENT"
  MODERATE:
    - "ROUTINE"
  LOW:
    - "INFORMATIONAL"
